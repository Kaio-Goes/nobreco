export default function TopBar() {
  return (
    <div className="bg-preto px-4 py-2 text-center text-[11px] font-medium uppercase tracking-wide text-off-white sm:text-xs">
      Pagamento no Pix com desconto · Entrega combinada direto no WhatsApp
    </div>
  );
}
